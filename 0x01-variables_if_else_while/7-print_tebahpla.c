#include <stdio.h>

/**
 *  *main - entry point
 *   *Description: random number in n +ve/-ve
 *    *Return: 0 on success
*/
int main(void)
{
char ch;
for (ch = 'z'; ch <= 'a'; ch++)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
